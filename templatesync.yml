hooks:
  precommit:
    commands:
      - echo "Running precommit cleanup…"
      - git restore .gitignore
      - git status
  prepush:
    commands:
      - echo "Running prepush cleanup…"
      - ./cdaas-workflow/scripts/create-merge-conflict-issue.sh
